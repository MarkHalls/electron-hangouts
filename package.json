{
  "name": "electron-hangouts",
  "version": "0.0.1",
  "description": "Standalone hangouts export reader for parsing data from google takeout.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {}
}
